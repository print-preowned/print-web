Admin portal scaffolding for Print.

## Getting Started

First, set the API URL and run the development server:

```bash
export NEXT_PUBLIC_PRINT_API_URL=http://localhost:8000
npm run dev
```

Open `http://localhost:3000/admin` to use the portal. Users and Roles have CRUD wired to the FastAPI endpoints.
